services:
  scanner:
    build: .
    container_name: ai-bug-bounty-scanner
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped